#+TITLE: Thoughts

Because crow has lazy evaluation for commands, generating to C is not straight forward. For the lazy evaluation to work, the C runtime requires an =eval= function, what should that function do? =eval= can either interpret the ast, or generate C code, the latter produces a much faster runtime but at the cost of first compilation.
