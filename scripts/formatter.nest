define:
  args = pop-front (command-line-arguments) 2

fun usage:
  print "formatter <file> [--out <path>]"

when (= (length args) 0):
  usage
  exit

with (open-file args.[0] "rb") in-file:
  define:
    content = (read-all in-file)
    ast = parse content
  print ast
