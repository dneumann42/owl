fun add-numbers(a, b) $(a + b)

fun factorial(n) do
  if n < 2 
  then 1
  else n * factorial(n - 1)
end

cond 
  x eq 1  print("HERE?")
  test()  do
    1 + 2
    stop
  end
end

x = fn(x) fn(y) x + y
